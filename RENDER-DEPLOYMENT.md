# 🚀 دليل نشر TrakoShip على Render

هذا الدليل يشرح كيفية نشر مشروع TrakoShip بالكامل مع قاعدة البيانات على Render.

## المتطلبات الأساسية

- حساب على [Render.com](https://render.com)
- مستودع GitHub للمشروع (تم رفعه بالفعل على: https://github.com/aladeebx/trraddk)

## الخطوات

### 1. إنشاء قاعدة بيانات PostgreSQL على Render

1. سجل الدخول إلى [Render Dashboard](https://dashboard.render.com)
2. اضغط على **"New +"** في الشريط العلوي
3. اختر **"PostgreSQL"**
4. املأ المعلومات التالية:
   - **Name**: `trakoship-db` (أو أي اسم تفضله)
   - **Database**: `trakoship` (أو اتركه افتراضياً)
   - **User**: سيتم إنشاؤه تلقائياً
   - **Region**: اختر أقرب منطقة لك
   - **PostgreSQL Version**: اختر أحدث إصدار (14+)
   - **Plan**: اختر الخطة المناسبة (Free tier متاح للتجربة)
5. اضغط على **"Create Database"**
6. انتظر حتى يتم إنشاء قاعدة البيانات (قد يستغرق دقيقة أو دقيقتين)

### 2. الحصول على DATABASE_URL

1. بعد إنشاء قاعدة البيانات، اضغط على اسمها للدخول إلى صفحة الإعدادات
2. في قسم **"Connections"**، ستجد **"Internal Database URL"**
3. انسخ هذا الرابط - سيكون بالشكل:
   ```
   postgresql://user:password@host:port/database?sslmode=require
   ```
4. احفظ هذا الرابط - ستحتاجه في الخطوة التالية

### 3. إنشاء Web Service على Render

1. في Render Dashboard، اضغط على **"New +"**
2. اختر **"Web Service"**
3. اختر **"Connect GitHub"** واربط حسابك مع GitHub
4. اختر المستودع: `aladeebx/trraddk`
5. املأ المعلومات التالية:
   - **Name**: `trakoship` (أو أي اسم تفضله)
   - **Region**: اختر نفس المنطقة التي اخترتها لقاعدة البيانات
   - **Branch**: `main`
   - **Root Directory**: اتركه فارغاً (أو `.` إذا طُلب منك)
   - **Runtime**: `Node`
   - **Build Command**: `npm install; npm run build`
   - **Start Command**: `npm start`

### 4. إضافة متغيرات البيئة

في صفحة إعدادات Web Service:

1. اذهب إلى قسم **"Environment"**
2. اضغط على **"Add Environment Variable"**
3. أضف المتغير التالي:
   - **Key**: `DATABASE_URL`
   - **Value**: الصق رابط قاعدة البيانات الذي نسخته في الخطوة 2
4. (اختياري) أضف متغيرات أخرى إذا لزم الأمر:
   - `NODE_ENV`: `production`
   - `PORT`: `10000` (Render يستخدم المنفذ 10000 افتراضياً)

### 5. ربط قاعدة البيانات مع Web Service

1. في صفحة إعدادات Web Service، اذهب إلى قسم **"Connections"**
2. اضغط على **"Link Resource"**
3. اختر قاعدة البيانات التي أنشأتها في الخطوة 1
4. سيتم ربطها تلقائياً

### 6. النشر

1. بعد إضافة جميع الإعدادات، اضغط على **"Save Changes"**
2. Render سيبدأ تلقائياً في بناء ونشر المشروع
3. يمكنك متابعة عملية البناء من تبويب **"Logs"**
4. بعد اكتمال البناء، سيتم بدء التطبيق تلقائياً

### 7. التحقق من النشر

1. بعد اكتمال النشر، ستحصل على رابط للتطبيق (مثل: `https://trakoship.onrender.com`)
2. افتح الرابط في المتصفح
3. يجب أن ترى صفحة TrakoShip الرئيسية
4. جرب تسجيل الدخول أو إنشاء حساب جديد

## ملاحظات مهمة

### حول قاعدة البيانات

- عند بدء التطبيق لأول مرة، سيقوم `prisma db push` تلقائياً بإنشاء جميع الجداول في قاعدة البيانات
- لا حاجة لتشغيل migrations يدوياً - كل شيء يتم تلقائياً
- إذا أردت إضافة بيانات تجريبية، يمكنك استخدام `npm run db:seed` محلياً ثم تصدير البيانات

### حول البناء

- تم تعديل سكريبت البناء لإزالة `prisma db push` (لأنه يحتاج DATABASE_URL)
- `prisma db push` يتم تشغيله الآن عند بدء التطبيق (`npm start`)
- هذا يضمن أن قاعدة البيانات متاحة عند تشغيل `db push`

### حول الأمان

- **لا تشارك DATABASE_URL** مع أي شخص
- Render يحمي متغيرات البيئة تلقائياً
- استخدم HTTPS دائماً (Render يوفره تلقائياً)

### استكشاف الأخطاء

#### خطأ: "Environment variable not found: DATABASE_URL"
- تأكد من إضافة `DATABASE_URL` في قسم Environment Variables
- تأكد من نسخ الرابط بشكل صحيح

#### خطأ: "Connection refused" أو "Cannot connect to database"
- تأكد من ربط قاعدة البيانات مع Web Service في قسم Connections
- تأكد من أن قاعدة البيانات تعمل (Status: Available)

#### خطأ: "Build failed"
- تحقق من Logs لمعرفة الخطأ الدقيق
- تأكد من أن Build Command صحيح: `npm install; npm run build`

#### التطبيق لا يعمل بعد النشر
- تحقق من Logs في قسم Runtime Logs
- تأكد من أن Start Command صحيح: `npm start`
- تأكد من أن قاعدة البيانات تم ربطها بشكل صحيح

## التحديثات المستقبلية

عند إجراء أي تغييرات على المشروع:

1. ارفع التغييرات إلى GitHub:
   ```bash
   git add .
   git commit -m "Your commit message"
   git push origin main
   ```

2. Render سيكتشف التغييرات تلقائياً ويبدأ عملية إعادة البناء والنشر

3. يمكنك متابعة العملية من Render Dashboard

## الدعم

إذا واجهت أي مشاكل:
- تحقق من [Render Documentation](https://render.com/docs)
- تحقق من Logs في Render Dashboard
- راجع ملف `SETUP-GUIDE.md` للتأكد من إعداد المشروع محلياً

---

**تم النشر بنجاح! 🎉**

